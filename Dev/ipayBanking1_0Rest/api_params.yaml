environments:
  - name: dev
    endpoints:
      production:
        url: https://www.google.com
        config:
          retryTimeOut: $RETRY
      sandbox:
        url: 
        config:
          retryTimeOut: $RETRY        
  - name: prod
    endpoints:
      production:
        url: https://www.google.com
        config:
          retryTimeOut: $RETRY      
      sandbox:
        url: 
        config:
          retryTimeOut: $RETRY      
